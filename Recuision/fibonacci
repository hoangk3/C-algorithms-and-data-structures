#include<bits/stdc++.h>
using namespace std;
int ppz( int n)
{
    if ( n == 1) return 1;
    if ( n == 2 ) return 1;
    return ppz( n -1 ) + ppz( n -2);
}
int main()
{
    int n;
    cin >> n;
    cout << ppz(n);
}
